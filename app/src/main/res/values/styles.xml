<?xml version="1.0" encoding="utf-8"?>

<resources>
    <style name="AppTheme" parent="Theme.AppCompat.DayNight"/>
    <style name="MyDatePicker" >
        <!-- Text color -->
        <item name="android:windowBackground">@drawable/shape_background</item>
        <item name="android:textColorPrimary">@color/nayablue</item>
        <!-- Divider color -->
        <item name="colorControlNormal">@color/nayablue</item>
    </style>

</resources>